import React from "react";
import { Button } from "./styles";

const ButtonPrimary = ({ className }) => {
  return <Button className={className}>Get Started For Free</Button>;
};

export default ButtonPrimary;
